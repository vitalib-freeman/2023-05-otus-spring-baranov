spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: always
      data-locations: data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

logging:
  level:
    ROOT: ERROR
